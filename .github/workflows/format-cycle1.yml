name: Cycle 1 formatter maintenance

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Formatting is enforced by the Cycle 1 quality gate."
