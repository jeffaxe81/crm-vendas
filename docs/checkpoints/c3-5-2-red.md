# C3.5.2 — RED checkpoint

Microentrega: API tenant-aware de Atividades.

Estado esperado deste checkpoint: RED funcional.

O branch contém o teste de integração da API antes da implementação de contracts/controller/service/module. O CI deve falhar porque `/api/v1/activities` ainda não existe. Falhas incidentais de formatação, lint ou typecheck não contam como RED funcional e devem ser corrigidas antes do início do GREEN.
